1. Porque arrow functions funcionam no botão remove mas function() não?
	- Porque a arrow function mantém o escopo do objeto;
2. Animações de fadeIn(Mount) e fadeOut(Unmount) para os componentes (Uso dos LifeCycle methods)?
3. Erro sobre key do Bruno, porque key={index} não está funcionando?
4. O bypass (CORS) é certo?
	- Não, pois o bypass causa uma grande falha de segurança
5. Alguma razão específica para static no arquivo JobList?
6. Existe alguma proteção nativa do Express para escape de caracteres ou é necessário implementar uma validação ou usar o express validator?
	- O ideal '
7. Com a inclusão do método representado pelo Auth, é possível dizer que pode-se encadear métodos para serem executados através de cada next?
8. "Para um componente ser statefull, precisa ter render()" não seria correto o componente ser statefull quando há manipulação do state?

